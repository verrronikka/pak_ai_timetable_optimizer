# Производительность solver (24.05.2026)

Источник метрик: `dev_alg/artifacts/benchmark_results.json`, эвристики по умолчанию (`static_mrv` + `combined`).

## Результаты по наборам

| Набор  | T×G | Задач | Шагов поиска | Время, с | Память, МБ |
|--------|-----|-------|--------------|----------|------------|
| tiny   | 4   | 8     | 9            | 0.04     | 0.002      |
| small  | 16  | 24    | 25           | 0.27     | 0.006      |
| demo   | —   | 24    | 25           | 0.42     | 0.034      |
| medium | 64  | 64    | 65           | 4.65     | 0.045      |
| large  | 144 | 96    | 97           | 18.61    | 0.227      |

Лимит прогона: `max_search_steps = 500_000`. Все наборы: `solve_status = успех`.

Итог актуального прогона: текущие ограничения остаются рабочими, потому что шаги поиска по-прежнему существенно ниже лимита.

## Рекомендации по лимитам API

Обновлено в `backend/solver_contraints.json`:

| Параметр  | Значение | Обоснование                                      |
|-----------|----------|--------------------------------------------------|
| `min`     | 5_000    | запас над medium (~65 шагов) для менее удачных входов |
| `default` | 200_000  | P95 шагов на large ≪ лимита; запас для откатов   |
| `max`     | 1_000_000| верхняя граница для ручного тюнинга              |

Ориентир по задачам: `max_search_steps ≥ task_count × 150` (large: 96 → 14_400, с большим запасом).

## Оптимизации backtracking (внедрено)

1. **MRV без `list.index`** — выбор задачи за один проход, splice вместо O(n) поиска индекса.
2. **Кэш загрузки слотов** (`_slot_load`) — сортировка кандидатов без `len(schedule[slot])` на каждом сравнении.
3. **Начальная сортировка** — degree (конкуренция teacher/group) + узость дней + тип аудитории.
4. **Порядок кандидатов `combined`** — загрузка слота → вместимость → нагрузка преподавателя.
